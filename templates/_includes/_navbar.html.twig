<nav class="navbar navbar-default" id="sticker">
    <div>
        <!-- Brand and toggle get grouped for better mobile display -->
        <div id="bs-example-navbar-collapse-1">
            <ul class="navbar-display-row navbar-nav">
                <li>
                    <a href="{{ path('app_home') }}">Home</a>
                </li>
                <li>
                    <a href="{{ path('app_listofcars') }}">Rent</a>
                </li>
                <li>
                    <a href="{{ path('app_about') }}">About Us</a>
                </li>
                <li>
                    <a href="{{ path('app_contact') }}">Contact Us</a>
                </li>
                {% if app.user %}
                    <li>
                        <a href="{{ path('app_logout') }}">Logout</a> <br>
                    </li>
                    <li>
                        <a href="{{ path('app_reservation') }}">My reservations</a> <br>
                    </li>
                    <li style ="align-self: end;">
                        <p>Welcome {{ app.user.fullName }} !</p>
                    </li>
                {% else %}
                    <li>
                        <a href="{{ path('app_loginpage') }}">Login</a>
                    </li>
                {% endif %}
                <li>
                    {% if is_granted("ROLE_ADMIN") %}
                        <a href="{{ path('admin') }}">Admin</a>
                    {% endif %}
                </li>
            </ul>
        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>